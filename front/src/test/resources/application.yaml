spring:
  cloud:
    # Отключаем Config Server
    config:
      enabled: false
    # Отключаем авторегистрацию в эрике
    service-registry:
      auto-registration:
        enabled: false
  config:
    import: optional:configserver:http://localhost:9999