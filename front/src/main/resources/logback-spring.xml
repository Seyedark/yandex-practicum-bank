<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <springProperty scope="context" name="SERVICE_NAME" source="spring.application.name"/>
    <springProperty scope="context" name="KAFKA_SERVER" source="spring.kafka.bootstrap-servers"/>
    <springProperty scope="context" name="KAFKA_TOPIC" source="kafka-log.topic.log"/>
    <keyingStrategy class="com.yourpackage.logging.KeyByServiceStrategy">
        <serviceName>${SERVICE_NAME:-unknown-service}</serviceName>
    </keyingStrategy>

    <!-- 1. Консоль -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level [%X{traceId:-}] [%X{spanId:-}] ${SERVICE_NAME:-unknown} %logger{36} - %msg%n
            </pattern>
        </encoder>
    </appender>

    <!-- 2. Синхронный Kafka Appender -->
    <appender name="KAFKA_SYNC" class="com.github.danielwegener.logback.kafka.KafkaAppender">
        <encoder class="com.github.danielwegener.logback.kafka.encoding.PatternLayoutKafkaMessageEncoder">
            <layout class="ch.qos.logback.classic.PatternLayout">
                <pattern>{
                    "timestamp":"%d{yyyy-MM-dd HH:mm:ss.SSS}",
                    "level":"%level",
                    "logger":"%logger{36}",
                    "thread":"%thread",
                    "message":"%replace(%msg){'\"', '\\\"'}",
                    "service":"${SERVICE_NAME:-unknown}",
                    "traceId":"%X{traceId:-}",
                    "spanId":"%X{spanId:-}"
                    }%n
                </pattern>
            </layout>
        </encoder>

        <topic>${KAFKA_TOPIC}</topic>
        <producerConfig>bootstrap.servers=${KAFKA_SERVER}</producerConfig>
        <producerConfig>acks=1</producerConfig>
        <producerConfig>retries=3</producerConfig>
        <producerConfig>max.block.ms=1000</producerConfig>
    </appender>

    <!-- 3. Асинхронный буфер для Kafka копит сообщения, а потом вызывает кафка синк по достижению какого-то из условий-->
    <appender name="ASYNC_KAFKA" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="KAFKA_SYNC"/>
        <queueSize>10000</queueSize>
        <maxFlushTime>5000</maxFlushTime>
        <neverBlock>true</neverBlock>
        <discardingThreshold>0</discardingThreshold>
    </appender>

    <!-- 4. Root logger -->
    <root level="INFO">
        <appender-ref ref="STDOUT"/>
        <appender-ref ref="ASYNC_KAFKA"/>
    </root>
</configuration>