spring:
  # Отключаем Flyway
  flyway:
    enabled: false
  kafka:
    bootstrap-servers: ${BOOTSTRAP_SERVERS_CONFIG:localhost:9092}
    topic:
      exchange: ${EXCHANGE_TOPIC:exchange-dto-list-topic}
    consumer:
      group-id: ${EXCHANGE_GROUP_ID:exchange}