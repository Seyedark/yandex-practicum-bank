replicaCount: 1

image:
  tag: "latest"
  pullPolicy: Never

service:
  type: ClusterIP
  port: 80
  targetPort: 8085

config:
  database_url: "jdbc:postgresql://postgres-0.postgres:5432/postgres?currentSchema=notification"
  data_base_fly_way_url: "jdbc:postgresql://postgres-0.postgres:5432/postgres"
  database_username: "postgres"
  database_password: "postgres"
  keycloak_url: "http://keycloak/keycloak/realms/master"
